<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.learnandroidoreo">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".NewsApp.ArticleActivity"></activity>
        <activity android:name=".NewsApp.NewsAppActivity" />
        <activity android:name=".webview.WebViewActivity" />
        <activity android:name=".SQLite.SQliteActivity" />
        <activity android:name=".Notes.NoteEditorActivity" />
        <activity android:name=".Notes.NotesActivity" />
        <activity android:name=".AlertDialog.AlertDialogActivity" />
        <activity android:name=".ActionBarMenu.ActionBarMenuActivity" />
        <activity android:name=".SharedPreferences.SharedPrefActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".maps.MemorablePlaceMapsActivity"
            android:label="@string/title_activity_memorable_place_maps" />
        <activity android:name=".maps.MemorablePlaceActivity" />
        <activity
            android:name=".maps.UserLocationOnMapsActivity"
            android:label="@string/title_activity_user_location_on_maps" />
        <activity android:name=".maps.LocationUpdateActivity" />
        <activity android:name=".maps.MainMapsActivity" />
        <activity
            android:name=".maps.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".Weather.WeatherShowActivity" />
        <activity android:name=".Weather.JSONParseActivity" />
        <activity android:name=".GuessTheCelebrity.GuessTheCelebrityActivity" />
        <activity android:name=".DownloadImage.DownloadWebActivity" />
        <activity android:name=".DownloadImage.DownloadImageActivity" />
        <activity android:name=".BrainTrainer.BrainTrainerActivity" />
        <activity android:name=".Timers.TimersActivity" />
        <activity android:name=".TimesTable.TimesTableActivity" />
        <activity android:name=".Audio.AudioActivity" />
        <activity android:name=".Video.VideoActivity" />
        <activity android:name=".GameConnect.GameConnectActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>